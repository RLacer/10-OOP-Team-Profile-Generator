// help us create the team by running some functions that output a string this is HTML
// const generateTeam = => {
//     return
// }

const generateManager = manager => {
    return`<div>HTML ${manager.getName()}</div>`}



module.exports = team => {
    return
}