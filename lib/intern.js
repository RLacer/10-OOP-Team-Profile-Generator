const Employee = require('./employee');

class Intern extends Employee {
  constructor(name, id, email, role) {
    super(name, id, email);
    this.school = school;
    const role = new role("Intern");
   
  }}